package org.sid.config;

import org.sid.entities.Domaine;
import org.sid.entities.Donnateur;
import org.sid.entities.Post;
import org.sid.entities.Role;
import org.sid.entities.Typepost;
import org.springframework.context.annotation.Configuration;
import org.springframework.stereotype.Component;

@Component
public class ExposeEntityIdRestConfiguration  {

	/*
	 * @Override public void
	 * configureRepositoryRestConfiguration(RepositoryRestConfiguration config) {
	 * 
	 * config.exposeIdsFor(Post.class); config.exposeIdsFor(Domaine.class);
	 * config.exposeIdsFor(Role.class); config.exposeIdsFor(Donnateur.class);
	 * config.exposeIdsFor(Typepost.class); }
	 */
}